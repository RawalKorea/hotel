---
description: 수정 시 버전 업데이트 및 Git 푸시 필수
alwaysApply: true
---

# 수정 후 버전·푸시 규칙

코드를 수정한 뒤에는 **반드시** 다음을 수행한다:

1. **버전 업데이트**
   - 패치(버그 수정 등): `npm run version:patch`
   - 마이너(기능 추가 등): `npm run version:minor`

2. **Git 커밋 및 푸시**
   - `git add -A && git commit -m "메시지" && git push origin main`
   - 또는 `npm run release:patch` / `npm run release:minor` 사용
   - 원격 인증은 로컬에 설정되어 있음 → 수정 후 푸시까지 반드시 수행

예외: 문서·주석·설정만 바꾸고 실제 기능 변경이 없을 때는 버전을 올리지 않아도 된다.
